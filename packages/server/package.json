{
    "name": "@cudos-private-sale/server",
    "scripts": {
        "build": "npx tsc -b",
        "dev": "ts-node-dev --respawn --pretty --transpile-only src/index.ts",
        "start": "npm run build && npx pm2 start build/index.js -i 2"
    },
    "dependencies": {
        "@onfido/api": "^2.6.0",
        "compression": "^1.7.4",
        "cors": "^2.8.5",
        "dotenv": "^16.0.3",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "@types/express": "^4.17.15",
        "ts-node-dev": "^2.0.0"
    }
}